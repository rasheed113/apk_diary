# Android 6 / API 23 compatibility

APK Diary requires Android 6.0 (API 23) as the minimum supported Android version.

The Android app module must keep `minSdk = 23` explicitly so the release APK remains installable on Android 6.0 devices.
