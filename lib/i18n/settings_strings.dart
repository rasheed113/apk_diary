import 'app_strings.dart';

/// Settings-page strings kept separate so the page can migrate safely.
/// Urdu and other languages will be added in the language layer later.
class SettingsStrings {
  SettingsStrings._();

  static const name = AppStrings.name;
  static const mobileNumber = AppStrings.mobileNumber;
  static const companyName = AppStrings.companyName;
  static const defaultJobType = AppStrings.defaultJobType;
  static const defaultMachineType = AppStrings.defaultMachineType;
  static const currency = AppStrings.currency;
  static const saveSettings = AppStrings.saveSettings;
}
